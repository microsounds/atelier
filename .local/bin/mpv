#!/usr/bin/env sh

# disable hardware acceleration on intel integrated gfx
unset opt
for f in $(glxinfo | tr 'A-Z' 'a-z' | egrep '^opengl (vendor|renderer)'); do
	case $f in
		intel) opt='laptop'; break;;
	esac
done

exec /usr/bin/mpv --profile="${opt:-desktop}" "$@"
