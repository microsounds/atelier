#!/usr/bin/env sh

# usage: swallow [command]
# swallow active window and return it after background process returns

[ ! -z "$1" ] || exit 1

WINID="$(xdotool getactivewindow)"
"$@" &
xdotool windowunmap "$WINID" && wait
xdotool windowmap "$WINID"

