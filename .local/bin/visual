#!/usr/bin/env sh

# Usage: visual [command]
# X window wrapper for terminal applications
# opens a new terminal if not open already

unset shim
if [ ! -z "$DISPLAY" ]; then
	case "$(tty)" in
		'not a tty') shim='urxvtc -e';;
	esac
fi

$shim "$@"
